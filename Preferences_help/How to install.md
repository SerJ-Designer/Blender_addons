Eng
____
### 🛠️ **How to Install and Use**

1. **Save the code**: Copy the code above and save it as a `.py` file, for example, `toggle_emulate_3button.py`.
    
2. **Install in Blender**:
    
    - Open Blender 5.0+.
        
    - Go to `Edit` → `Preferences` → `Add-ons`.
        
    - Click `Install...`, select the `.py` file, and enable the addon by checking its box.
        
3. **Two Ways to Use It**:
    
    - **Top Bar Button**: After installation, a new button will appear in Blender's top bar, to the right of the standard menus (like Edit, Render). Click it to toggle the setting on or off[](https://extensions.blender.org/add-ons/emu3bm/)
        

- .
    
- **Keyboard Shortcut**: The default shortcut is **`Ctrl+Shift+Alt+E`**. You can change this in `Edit` → `Preferences` → `Keymap` → `Window` if needed[](https://docs.blender.org/manual/en/latest/editors/preferences/keymap.html)
    



Ru

---

### 🛠️ **Как установить и использовать**

1. **Сохраните код**: Скопируйте код выше и сохраните его как файл `.py`, например `toggle_emulate_3button.py`.
    
2. **Установите в Blender**:
    
    - Откройте Blender 5.0+.
        
    - Перейдите в `Edit` → `Preferences` → `Add-ons`.
        
    - Нажмите `Install...`, выберите файл `.py` и включите аддон, установив флажок.
        
3. **Два способа использования**:
    
    - **Кнопка в верхней панели**: После установки в верхней панели Blender (справа от стандартных меню) появится новая кнопка. Нажимайте её для переключения настройки.
        
    - **Горячая клавиша**: Сочетание по умолчанию — **`Ctrl+Shift+Alt+E`**. Его можно изменить в `Edit` → `Preferences` → `Keymap` → `Window`.
        

